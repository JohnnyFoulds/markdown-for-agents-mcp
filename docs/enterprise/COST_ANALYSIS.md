# Cost of Ownership Analysis — markdown-for-agents-mcp

> **Status: complete.** All figures are generated by `scripts/cost-model.py` and committed
> in `scripts/cost-model-output.json`. Retrieved vendor prices are dated in that file;
> re-run the script if constants are older than 180 days.
> See `src/costAnalysis.test.ts` for the consistency guard.

---

## 1. Headline recommendation

**Continue, but reposition — and right-size before deploying to production.**

The economic case for self-hosting this tool is *not* cost. At the shipped ECS
topology the cost case is decisively negative. At a right-sized single-task
deployment it becomes marginal. The genuine case is:

1. **POPIA s72 data residency** — both Tavily and Firecrawl are US-hosted SaaS; for a
   regulated South African buyer (FSP, MNO) that makes them non-options at any price for
   sensitive workloads, without a completed cross-border transfer assessment.
2. **MIT licence** — Firecrawl's self-hosted stack is AGPL-3.0, which carries licence
   obligations in a commercial product deployment. This repo is MIT with 7 runtime
   dependencies.
3. **Footprint** — 3-service deployment (server + worker + store) vs Firecrawl's 6–7
   (API + workers + Playwright + Redis + RabbitMQ + PostgreSQL + optional FoundationDB),
   which is unauthenticated by default.
4. **Governance pack** — 14 enterprise documents (POPIA assessment, threat model, ToS
   analysis, SLO, runbook, DFD, ATO, etc.) that regulated buyers need for sign-off.
   No commercial vendor ships this for an SA buyer; it is a delivered asset, not a
   competitive claim.

Cost **is not a positive differentiator** for any realistic SA enterprise workload
below approximately 4 million pages per month with 0.25 FTE assigned. The decision
tree is:

```
Workload requires POPIA s72 residency OR internal hosts?
├── YES → Self-host (or buy nothing). Cost is not the question; buy is not an option.
└── NO  → Is monthly volume > 4M pages AND do you have 0.25+ FTE to maintain?
          ├── YES → Evaluate self-hosting on the right-sized topology
          └── NO  → Buy Firecrawl Standard/Scale. Lower cost, lower risk.
```

**The hybrid approach is the most defensible default:** Firecrawl for public-web bulk
(where they absorb bot-defence and volume), self-hosted render retained for
internal/residency-bound URLs (where the compute bill is low and IP-reputation risk is
zero). This approach eliminates the high-volume throughput uncertainty that makes the
confidence interval wide, and preserves the governance layer — the durable asset.

---

## 2. Scope and method

- **Question 1 (buyer TCO):** Should a regulated SA enterprise use this tool or buy
  Tavily/Firecrawl?
- **Question 2 (developer TCO):** Should this tool continue to be developed?
- **These are different questions.** Sunk cost is excluded from both by design —
  the question is go-forward cost. That is correct decision theory, and it is also the
  framing least favourable to the existing codebase.
- All prices are **list prices, retrieved 2026-08-24** (see `scripts/cost-model.py`
  for `source`/`retrieved` fields). Negotiated enterprise discounts on *both* sides
  would move the analysis; the direction is noted where material.
- All infrastructure costs are computed at **af-south-1 (POPIA-residency case)** unless
  noted. af-south-1 is +35% on Fargate and +19% on EC2 vs eu-west-1.
- USD primary; **ZAR/USD 18.5** (assumption — see §13).

---

## 3. What dominates: the total pipeline cost

**Retrieval is approximately 4% of total pipeline cost** at typical agent workloads.

At 100 000 pages/month (20 000 queries/month, 5 pages/query):

| Component | Self-host (right-sized) | Firecrawl Standard |
|---|---|---|
| LLM inference (Claude Sonnet, first-party) | ~$1 500/mo | ~$1 500/mo |
| Retrieval / infra | $251/mo | $83/mo |
| Engineering (0.1 FTE) | $667/mo | $0 |
| **Total** | **~$2 418/mo** | **~$1 583/mo** |

> ⚠ **LLM costs shown at first-party Claude Sonnet pricing ($3/$15 per MTok input/output).
> Bedrock rates differ — verify at `aws.amazon.com/bedrock/pricing/`.**
>
> See `docs/enterprise/assets/cost-chart3-pipeline-composition.png`.

The retrieval cost difference ($168/mo) is 11% of the LLM bill.

**Consequence:** A ±20% difference in extracted-token verbosity between self-hosted
markdown extraction and Tavily's `raw_content` is worth approximately $300/mo —
nearly 2× the entire retrieval cost differential. **Token efficiency is the most
important unmeasured variable in this analysis** (see §12 — the one missing
measurement).

---

## 4. Verified vendor pricing (retrieved 2026-08-24)

### 4.1 Tavily

Source: `https://tavily.com/#pricing`

| Tier | Credits/mo | Price/mo | Unit rate |
|---|---|---|---|
| Researcher | 1 000 | Free | — |
| Project | 4 000 | $30 | $0.0075/credit |
| Bootstrap | 15 000 | $100 | $0.0067 |
| Startup | 38 000 | $220 | $0.0058 |
| **Growth** | **100 000** | **$500** | **$0.005** |
| Pay-as-you-go | — | — | $0.008/credit |

Credit costs: basic search **1 credit**, advanced search **2 credits**, basic extract
**1 credit per 5 URLs** ($0.001/URL at Growth), crawl **~0.3 credits/page**. Failed
extractions and maps are **not charged** — an asymmetry that favours buying over
self-hosting on pages that fail.

### 4.2 Firecrawl

Source: `https://www.firecrawl.dev/pricing` (annual billing)

| Tier | Pages/mo | Price/mo | Unit rate |
|---|---|---|---|
| Hobby | 5 000 | $16 | $0.0032/page |
| **Standard** | **100 000** | **$83** | **$0.00083** |
| Growth | 500 000 | $333 | $0.000666 |
| Scale | 1 000 000 | $599 | $0.000599 |

Monthly billing is ~+20% over the annual equivalent. Failed scrapes are not charged.

Firecrawl's global cache has multi-day TTLs — significantly better than this repo's
per-process 50 MiB/15-min LRU (see §8.3).

> ⚠ **Firecrawl is AGPL-3.0.** Its self-hosted stack (API + workers + Playwright +
> Redis + RabbitMQ + PostgreSQL) is unauthenticated by default with no persistent
> volumes. AGPL-3.0 creates licence obligations in commercial deployment. This repo
> is MIT with 7 runtime dependencies.

### 4.3 Brave Search API

Source: `https://brave.com/search/api/` — **$5 per 1 000 requests = $0.005/query**

At the Growth tier, Brave equals Tavily exactly in per-query cost. Enabling both
in parallel (which is the default when both API keys are set, via RRF merge)
doubles the per-query spend. **Search workloads never favour self-hosting over
Tavily at equal quality.**

---

## 5. Infrastructure costs (af-south-1)

All computed by `scripts/cost-model.py`. Key rates:

| Item | af-south-1 | eu-west-1 | us-east-1 |
|---|---|---|---|
| Fargate vCPU-hr | $0.0546 | $0.04048 | $0.04048 |
| Fargate GiB-hr | $0.0060 | $0.004445 | $0.004445 |
| NAT Gateway | $0.045/hr + $0.045/GiB | same | same |
| EKS control plane | $0.10/hr ($73/mo) | same | same |
| EC2 m6i.xlarge | $185/mo | $156/mo | $140/mo |

af-south-1 is +35% on Fargate vs eu-west-1. `m7i` generation is not available there —
POPIA residency means paying more for older silicon.

---

## 6. Deployment mode cost inventory

| Mode | Description | Monthly cost (af-south-1) |
|---|---|---|
| A | Local stdio / npx / CLI | $0 (no server) |
| B | Single VM, Docker Compose, no HA | $74/mo (m6g.large) |
| C | Single VM, `--role=both`, SQLite | $74/mo |
| **G** | **Existing OpenShift + in-house proxy + free providers** | **$0 infra (see §6.3)** |
| **F-right-sized** | **Single Fargate task, SQLite, 1 NAT GW** | **$251/mo** |
| F-right-sized+Spot | Same, Fargate Spot workers | $113/mo |
| E | K8s, 3×m6i.xlarge, EKS + NAT + ALB | $682/mo |
| **F-shipped** | **ECS as shipped (desired: 3 server, 2 worker)** | **$863/mo** |
| F-shipped HPA-max | ECS at autoscale maximum (20 server, 50 worker) | **$12,249/mo** |

> **The shipped ECS topology bills $863/mo before serving a single request.**
> At autoscale maximum it reaches $12,249/mo with no spend cap (14.2× ceiling).
> The `scaling.json` HPA bounds (server max=20, worker max=50) are not just capacity
> parameters — they are an uncapped liability.
>
> See `docs/enterprise/assets/cost-chart2-waterfall.png`.

### 6.1 Cost stack (ECS desired, af-south-1)

| Component | Monthly |
|---|---|
| Fargate compute (3 server + 2 worker) | $691 |
| NAT Gateway (3 AZ × $0.045/hr) | $99 |
| ALB | $16 |
| ElastiCache (mandatory — `STORE_BACKEND=redis` hardcoded) | $50 |
| CloudWatch Logs | $3 |
| **Total** | **$859** |

### 6.2 Right-sizing saves ~$750/mo

| Step | Remaining monthly cost |
|---|---|
| Start: Shipped ECS desired | $863 |
| Drop 2 of 3 NAT Gateways (keep 1) | ~$773 |
| Drop ElastiCache (use SQLite store) | ~$723 |
| Scale to single Fargate task, role=both | **$251** |
| Apply Fargate Spot | **$113** |
| Firecrawl Standard (100k pages, annual) | $83 |

A two-day engineering effort to right-size the manifests would drop the self-host
floor from $863 to $251 — and to $113 with Spot, which the worker (lease-based queue
consumer) architecturally earns. This does not deliver HA, but neither does the
shipped topology: the 3-replica ECS deployment bottlenecks availability on a single
ElastiCache node with no Multi-AZ configuration.

### 6.3 Mode G: Existing OpenShift + in-house proxy + free providers

This is the deployment scenario for an enterprise that already operates OpenShift
and already owns a corporate web proxy for internet egress. Every cloud cost line
item disappears:

| Component | Cloud (ECS af-south-1) | Mode G (existing OpenShift) |
|---|---|---|
| Compute | $691/mo | $0 (existing nodes) |
| NAT Gateway | $99/mo | $0 (existing proxy) |
| Load balancer | $16/mo | $0 (OpenShift Routes) |
| Cluster control plane | $73/mo | $0 (existing OpenShift) |
| Managed Redis | $50/mo | $0 (SQLite or existing Redis) |
| Logging/metrics | $3/mo | $0 (existing EFK/Prometheus) |
| Vendor search fees | $0 (free providers) | $0 (same) |
| **Total infra** | **$863/mo** | **$0** |

**Engineering is the only cost**, and it is lower than in a cloud scenario because
there is no infrastructure to manage. With mature CI/CD (automated image builds,
test gates, streamlined change approval for dependency updates), the ongoing
maintenance reduces to periodic PR reviews:

| Phase | FTE | Duration | Cost |
|---|---|---|---|
| Initial setup (Routes, SCC, proxy, KEDA) | 0.15–0.25 FTE | 10 weeks once | ~$4,274 amortised over 3 yr → $427/mo |
| Ongoing (Playwright updates, Dependabot reviews) | **0.015 FTE** | Continuous | **$100/mo** ($1,200/yr = R22,200/yr) |

The 0.015 FTE floor corresponds to approximately **10–15 hours per year** of a
senior engineer's time: reviewing Playwright release notes, approving automated
Dependabot PRs once CI is green, and handling occasional incidents.

**Total ongoing after setup amortisation ends (year 4+): $100/mo = R1,850/mo.**

> ⚠ **Three caveats apply regardless of cost:**
>
> 1. **Disable DuckDuckGo.** Set `SEARCH_ENABLE_DUCKDUCKGO=false`. DDG's ToS prohibits
>    automated scraping; this is documented in `docs/enterprise/TERMS_OF_SERVICE.md`.
>    Use `SEARXNG_ENGINE_PROFILE=clean` (Mojeek + Marginalia + Brave free), which
>    permits automated access. This results in lower recall on ambiguous queries —
>    see `docs/enterprise/SLO.md` Ceiling #2. This is a product gap, not a cost gap.
>
> 2. **Chromium does not automatically respect environment proxy settings.** Playwright
>    launches Chromium with explicit flags. To route through the in-house proxy,
>    `--proxy-server=<host>:<port>` must be added to the Chromium launch arguments in
>    `src/render/tiers/playwrightTier.ts` (or `src/render/browserPool.ts`). The HTTP
>    tier (Tier 1) respects `HTTPS_PROXY` / `http_proxy` env vars via undici. Set these
>    in the ConfigMap; also add `--proxy-server` for Tier 3.
>
> 3. **OpenShift SecurityContextConstraints.** Worker pods launch Chromium, which
>    requires a custom SCC or the `--no-sandbox` flag. In a dedicated namespace with
>    trusted workloads, `--no-sandbox` is acceptable; in a multi-tenant cluster, scope
>    a custom SCC to the `mcp-system` namespace. Document the choice.

---

## 7. Break-even analysis

Self-host break-even against **Firecrawl Scale** ($0.000599/page at 1M pages) —
monthly volume above which self-host total cost is lower.

| Topology | FTE | Break-even vs Firecrawl | Pages/day |
|---|---|---|---|
| Shipped ECS | 0.06 (cloud floor) | ~3.0M/mo | ~99 000 |
| Shipped ECS | 0.25 FTE | ~5.1M/mo | ~170 000 |
| Right-sized Fargate | 0.06 (cloud floor) | ~1.9M/mo | ~65 000 |
| Right-sized Fargate | 0.25 FTE | ~4.1M/mo | ~135 000 |
| **OpenShift (Mode G)** | **0.015 FTE (floor)** | **~1.0M/mo** | **~34 000** |
| OpenShift (Mode G) | 0.10 FTE | ~2.0M/mo | ~66 000 |
| OpenShift (Mode G) | 0.25 FTE | ~3.6M/mo | ~121 000 |

**Mode G changes the economics materially.** The break-even floor drops from
65 000 pages/day (right-sized Fargate) to 34 000 pages/day (OpenShift). More
importantly, for **search-only workloads** the break-even is **the first query**:
Mode G uses free providers ($0/query) vs Tavily Growth ($0.005/credit); any search
volume above zero is cheaper on Mode G, and engineering is the only cost to cover.

> ⚠ **Cloud scenarios (ECS, K8s):** The 0.06 FTE cloud floor includes infrastructure
> management overhead. **OpenShift scenario:** The 0.015 FTE floor assumes mature CI/CD
> where Dependabot PRs are auto-approved on green tests and Playwright updates take
> ~30 min each. If Vodacom's change-management process requires manual approval per
> dependency update at 4–8h each, the floor is closer to 0.03–0.05 FTE — still
> substantially lower than the cloud scenario.
>
> See `docs/enterprise/assets/cost-chart5-breakeven-fte.png` and
> `docs/enterprise/assets/cost-chart1-ratio-map.png`.

### 7.1 The rate-limit ceiling

`RATE_LIMIT_PER_HOST_RPS=2` (default, `deploy/k8s/base/configmap.yaml`) is a shared
bucket across replicas. The maximum sustainable rate on a single domain is 2 req/s =
5.2M pages/month — which is right at the break-even threshold for the right-sized
topology at 0.25 FTE. A concentrated workload (one vendor portal, one regulatory site)
cannot physically reach the volume that would justify self-hosting without a multi-host
distribution strategy. The break-even analysis assumes an unbounded domain set.

---

## 8. Hidden-cost register

These are the costs that are commonly omitted from first-pass comparisons.

### 8.1 Failed-fetch asymmetry

Tavily and Firecrawl charge nothing for failed scrapes. Self-hosting pays full compute
for every attempt. Assumption: 25% failure rate on public web (bot-blocking, timeouts).
Effect: true self-host cost per successful page is ~33% higher than the infra rate
implies. Vendor costs are unaffected.

### 8.2 Duty-cycle asymmetry

SaaS is pay-per-use. Self-hosting pays for reserved capacity 24×7. A business-hours
workload concentrated in 9–18 SAST has a ~10% duty cycle. At 10% duty cycle, a
$251/mo self-host deployment costs ~$25 of capacity that is actually doing work; the
other $226/mo is idle reservation. Vendors charge nothing for idle periods.

At 40% duty cycle (schedulable batch), the self-host model improves substantially —
this is the topology's genuine advantage case.

### 8.3 Cache asymmetry

`urlCache` in `src/fetcher.ts` is a **per-process 50 MiB / 15-minute LRU**. In a
3-replica deployment behind round-robin with no session affinity, the effective cache
hit rate is approximately 1/N of the single-instance rate (divided across replicas).
At HPA max (20 servers), effective hit rate is near zero.

Firecrawl's global cache is shared across all customers with multi-day TTLs.
**Cache favours buying, not self-hosting.** The ~40% steady-state hit rate target in
`docs/enterprise/SLO.md` is not achievable with the current per-process architecture.

A shared page cache (backed by Redis/Valkey) is a missing artifact, not a free credit.

### 8.4 IP reputation risk

Self-hosted crawls originate from the enterprise's egress IPs. Enterprise NAT EIP
ranges placed on a Cloudflare/Akamai bot blocklist is an operational incident with
no direct cost line, but a non-trivial recovery effort. Firecrawl and Tavily amortise
this risk across proxy pools.

### 8.5 NAT Gateway egress (RENDER_BLOCK_RESOURCES)

With `RENDER_BLOCK_RESOURCES=true` (default, recommended): ~0.5 MiB/page through NAT.
At 1M pages/mo: 500 GiB × $0.045/GiB = $22.50. At `false`: ~2 MiB/page → $90/mo.
The flag is a real cost control. Note: it is not set in
`deploy/k8s/base/configmap.yaml` — defaults to `true` in `src/config.ts:89`, but
can be overridden, and setting it to `false` also OOMKills the server pods due to
higher Chromium memory consumption.

### 8.6 Compliance cost on the buy side

Buying a US-hosted SaaS into an SA FSP or MNO triggers, at minimum:
- POPIA s72 cross-border transfer justification + s21 operator agreement/DPA
- FSCA/PA Joint Standard 1 of 2023 and 2 of 2024 third-party assessment
- Vendor security questionnaire, SOC 2 review, financial-viability check

This is plausibly 2–6 months of legal/security-architect calendar time, at rates above
a senior engineer's, and for some data classes may be refused outright. This cost is
typically modelled at $0 on the buy side, which biases the comparison.

### 8.7 Vendor pricing risk

Tavily and Firecrawl are venture-funded and pre-profitability. A 3× price move or
acquisition is a real option-value consideration. Self-hosting caps that exposure.
Conversely, the list-price ceiling used throughout this analysis may overstate actual
enterprise pricing — 30–50% off a published Growth/Scale tier is routine at volume.
**Negotiated vendor discounts move the break-even further against building.**

### 8.8 FX volatility

Infrastructure: USD. SaaS: USD. Engineering: ZAR. ZAR/USD has swung ±15–20%
annually. ZAR depreciation makes SaaS relatively more expensive; appreciation makes
it cheaper. This is a genuine directional asymmetry larger than NAT, CloudWatch, and
ECR combined, and larger than most of the deployment-mode differences in §6. The
ZAR/USD assumption is a named sensitivity parameter, not a fixed fact.

---

## 9. Engineering cost

Engineering floors depend on the deployment context.

**Cloud scenario (ECS / new K8s cluster) — 0.06 FTE floor** (~60h/yr):
- ~10–15 Chromium CVEs/yr requiring image rebuild + regression + change approval
  at 4–8h each in a regulated enterprise with manual change processes
- Playwright monthly release cadence + Dependabot triage
- Infrastructure management (node scaling, NAT, ALB, ElastiCache)

**Existing OpenShift scenario (Mode G) — 0.015 FTE floor** (~15h/yr):
- Playwright/Chromium updates ~4–6 times/year at ~30 min each = ~3h/yr
- Dependabot PR review when CI is green: ~5–10 min per batch = ~5h/yr
- Occasional incident: ~5h/yr
- **No** infrastructure management (nodes, proxy, metrics already exist)
- The 4–8h figure only applies when change management requires manual approval per
  dependency. With automated CI gates and batch approval, it drops to ~30 min/update.

**0 FTE is not an admissible scenario in either case.** The break-even chart's
y-axis starts at the relevant floor, not at zero.

At 0.25 FTE (central estimate), monthly engineering cost is:

| Component | Monthly |
|---|---|
| Amortised setup (12 weeks, over 3 years) | $384 |
| Ongoing maintenance (0.25 FTE, $80k/yr) | $1 667 |
| **Total** | **$2 051** |

At 0.25 FTE, right-sized ($251 infra + $2 051 eng = $2 302/mo), self-hosting is more
expensive than **Firecrawl Scale** until ~4.1M pages/month. The engineering cost
dominates the infra cost by 8×.

---

## 10. The non-cost case

For the target buyer (regulated SA enterprise), these factors are determinative
regardless of price:

### 10.1 POPIA s72 residency

Both Tavily and Firecrawl are US-hosted SaaS. For a regulated SA buyer, sending
queries and fetched content to a US provider requires:
- A completed POPIA s72 cross-border transfer assessment
- A contractual DPA with the vendor
- Likely approval from Legal and DPO

For sensitive content (internal documents, customer data, regulatory filings), this
process may conclude with a refusal — **making the vendor non-viable at any price**.
The cross-border analysis in `docs/enterprise/POPIA_ASSESSMENT.md` and
`docs/enterprise/DATA_FLOW.md` applies equally to Tavily and Firecrawl.

This factor completely dominates price for regulated buyers. For unregulated buyers
(internal tooling, no sensitive data), it is negotiable.

### 10.2 Archetype C: internal hosts

Internal intranet, SharePoint, regulatory documents behind the corporate perimeter
cannot be sent to a US SaaS at any price. For this workload segment, the alternative
is not "buy for $83/mo" — it is "build or go without."

The compute bill for internal URLs is low (no bot-blocking, 100% success rate, low
NAT costs, domain-limited throughput). This is where self-hosting's unit economics
are most favourable.

### 10.3 Governance pack as a delivered asset

The 14 enterprise documents in `docs/enterprise/` reduce the buyer's own ATO
(Authority to Operate) cost. A regulated buyer building this capability themselves
would spend 3–6 months on the governance pack alone. **This is not a claim about
the software quality — it is a statement about what the buyer would need to produce
independently.**

Firecrawl self-hosted is unauthenticated by default. It would fail the same governance
gate this repo is built to pass, and would carry the same ATO burden.

---

## 11. Explicit recommendation

**Build: the governed MCP interface and internal-host render path.**
**Buy: Firecrawl for public-web bulk extraction at high volume.**

Specifically:

1. **Keep developing the MCP interface, governance layer, and POPIA controls** — this
   is the part with no market substitute for an SA regulated buyer. The POPIA
   assessment, per-engine ToS analysis, audit path, data-residency controls, and
   ATO documentation are a delivered asset.

2. **Retain self-hosted Chromium render for internal/residency-bound URLs** — the
   cost is low, the residency guarantee is absolute, IP-reputation risk is nil.

3. **Route public-web bulk extraction to Firecrawl** — accept the vendor risk; route
   via this repo's MCP interface so the governance and audit layer applies regardless
   of whether render happens locally or remotely.

4. **Right-size the shipped manifests before production deployment** — the ECS topology
   as shipped at $863/mo is 3.4× the cost of a viable right-sized topology ($251) and
   is 10× more expensive than Firecrawl Standard for comparable volume. This is a
   two-day effort.

### 11.1 Mode G: for enterprises already on OpenShift

If the deploying organisation already runs OpenShift and already owns a corporate
web proxy, the calculus changes significantly:

**Deploy Mode G (§6.3) for all workloads, buy nothing.**

At ~$100/mo ongoing engineering cost ($1,200/yr = R22,200/yr after setup amortisation
ends) with $0 marginal infra, the cost argument for buying Firecrawl disappears at any
meaningful search or extract volume. The POPIA and governance arguments remain
decisive, not supplemental.

The decision tree simplifies to:

```
Already on OpenShift + have in-house proxy?
├── YES → Deploy Mode G.
│         Set SEARCH_ENABLE_DUCKDUCKGO=false.
│         Set SEARXNG_ENGINE_PROFILE=clean.
│         Configure --proxy-server for Chromium in browserPool.ts.
│         Add OpenShift Route + SCC for worker pods.
│         Wire KEDA for HPA (OpenShift Custom Metrics Autoscaler Operator).
│         Total setup: ~10 weeks. Ongoing: ~15h/yr.
└── NO  → Use right-sized Fargate ($251/mo) or evaluate buy vs build on §7 numbers.
```

For **search-only workloads**: Mode G beats Tavily at the first query. The search
providers are free; the infra is paid for. Engineering is the only cost, and at
R22,200/yr it is well below any Tavily tier above Researcher.

For **extract/crawl workloads on public web**: Mode G beats Firecrawl Standard ($83/mo)
once engineering exceeds ~1M pages/month at the 0.015 FTE floor — which at 10% duty
cycle corresponds to roughly 34 000 pages/day. Below that, Firecrawl Standard is
marginally cheaper in direct cost but loses on residency, governance, and licence.

For **internal/residency-bound URLs**: no vendor alternative exists at any price.
Mode G is the only option.

### Conditions under which the recommendation flips

**"Buy Firecrawl entirely" if:**
- Deployer is NOT on existing OpenShift (cloud-only, paying for infra from scratch)
- Volume is below 1M pages/month AND data is not residency-sensitive
- The buyer has no POPIA s72 constraint AND no AGPL licence restriction
- Engineering capacity is below 0.015 FTE even with automated CI/CD

**"Expand self-host aggressively" if (OpenShift scenario):**
- Already deployed and working for internal URLs
- Token-efficiency benchmark (§12) confirms ≥20% verbosity advantage
- The ATO is signed and on-call rota is in place
- No reason remains to route public-web bulk through a third party

---

## 12. The one missing measurement

**The single measurement that would most change this analysis is a token-efficiency
and extraction-quality benchmark:**

Run the same 200–500 representative URLs through:
- This tool (via `fetch_url`)
- Tavily basic extract
- Firecrawl scrape

Measure:
- Output tokens per page (via `messages.count_tokens` or equivalent)
- Success rate
- An LLM-judged answer-quality score on a fixed question set

**Why this matters more than everything else:** A ±20% extraction-verbosity
difference is worth ~$300/mo at 100k pages — larger than the entire retrieval
cost differential — and grows with volume. If this tool's markdown is 25%+ tighter
than the vendors' at equal answer quality, the build case improves across every
deployment mode. If it is 10%+ more verbose, buying wins even in the POPIA-constrained
case (where a local Firecrawl self-host is the alternative).

**Cost: ~1 day, ~$50 in API credits. No cluster required.**

**Second priority:** Extend `scripts/load-test.mjs` to ramp concurrency until p95
latency breaches and report pages/second at the saturation point plus which resource
saturated (memory, CPU, `BROWSER_MAX_JOBS=50` recycles, or the rate-limit bucket).
This converts two of the model's four assumed inputs to measured values.

---

## 13. Load-bearing assumptions

All `confidence=assumed` inputs. The model is only as reliable as these inputs.

| Parameter | Value | Sensitivity | Resolution |
|---|---|---|---|
| **Render latency** | 4 s/page (range: 2–8 s) | HIGH — sets throughput ceiling | Extend `scripts/load-test.mjs` |
| **Success rate** | 0.75 (75%) | HIGH — failure asymmetry | Extend load test |
| **Duty cycle** | 10% | VERY HIGH — self-host pays 24×7 | Measure workload profile |
| **LLM tokens/page** | 4 000 | CRITICAL — 25× larger than all infra | Token-efficiency benchmark (§12) |
| **ZAR/USD** | 18.5 | MEDIUM | Named parameter in model |
| **Engineering rate** | $80k/yr fully-loaded | MEDIUM | Adjust for actual rate |

> A model with an unresolved confidence interval on any of the top three inputs
> cannot produce a precise crossover volume. The analysis gives a band. Anyone who
> quotes a single break-even number from this document should be asked which
> throughput assumption they used.

---

## 14. Errata — corrected claims in this repository

The following claims from existing documents are corrected here.

### 14.1 `docs/enterprise/ENTERPRISE_READINESS.md` (former §"Zero per-query cost")

**Former claim:** "At 10 000 searches per day, Tavily costs roughly R650 000 per year
at ~$0.01/search. This tool's marginal cost is compute — which the platform team
already pays for."

**Three errors:**
1. **Tavily overstated ~2×**: $0.01/search is the PAYG rate ($0.008) or a tier premium.
   The Growth tier is **$0.005/credit**, and a 10k/day = 3.65M credits/year buyer would
   be on a custom Enterprise plan likely below $0.005. The correct Growth-tier
   comparison is ~R170k/year, not R650k.
2. **"Zero per-query cost" is false**: If `BRAVE_API_KEY` is set, egress costs
   $0.005/query — identical to Tavily Growth. The repo fans out providers in parallel
   with RRF merge; enabling Brave and Serper together doubles per-query spend.
3. **"which the platform team already pays for" is the free-compute fallacy**: at the
   shipped ECS topology the fixed cost is **$863/mo before any traffic** (see §6).

This section has been rewritten in `ENTERPRISE_READINESS.md` to state the accurate
position (see Phase 4 of this analysis).

### 14.2 `FUTURE_WORK.md` (Brave price)

**Former claim:** "~$0.003/search"
**Correct rate:** $5 per 1 000 requests = **$0.005/search** (verified 2026-08-24,
`https://brave.com/search/api/`). This matches `README.md:610` ($5/1k). The
discrepancy has been corrected.

### 14.3 `TAVILY_PARITY_PLAN.md` (Brave + Serper cost estimate)

**Former claim:** "Brave + Serper fan-out ≈ $8–12/1k searches"
**Verified rate:** Brave = $5/1k = $0.005/search. Serper pricing not publicly
available (see `src/search/providers/serper.ts`). The $8–12 estimate may be the
*combined* fan-out; the constituent Brave rate is $5/1k.

---

## 15. Ceilings

1. **Throughput is unmeasured.** Every self-host unit cost inherits that uncertainty.
   See §12 and §13. Quote a band, not a point.
2. **List prices are not contract prices.** AWS Savings Plans / EDP can cut the
   self-host figure; negotiated vendor discounts cut the buy figure. Both directions
   are possible; the model gives the list-price case.
3. **A $74/mo VM is not comparable to a vendor SLA.** No-HA is the cheap floor for
   comparison purposes; it is not a production recommendation. HA topologies are
   the honest comparison and they are the expensive ones.
4. **The ATO is a calendar risk, not a cost risk.** `PRODUCTION_AUTHORISATION.md`
   reads NOT AUTHORISED. Legal/DPO/security-architect time in an SA enterprise is
   3–6 months calendar. The buy option ships in an afternoon behind an existing
   egress proxy. Time-to-value is the strongest buy argument; it is not a chart.
5. **Cost is not the deciding factor.** If POPIA s72 makes both vendors unusable,
   the crossover analysis is informative but not determinative. The real question
   becomes self-host vs Firecrawl self-host vs no capability at all.
6. **RERANK_BACKEND=local fails at runtime.** Both ECS task definitions set
   `RERANK_BACKEND=local`, but `@huggingface/transformers` and `onnxruntime-node`
   are absent from `package.json` and `package-lock.json`. The shipped HPA custom
   metrics (`mcp_inflight_requests`, `crawl_queue_depth`) cannot drive autoscaling
   without a `prometheus-adapter` no manifest installs. These are setup costs in
   addition to the infrastructure costs modelled here.

---

*All figures generated by `scripts/cost-model.py`. Re-run to refresh if input prices
are stale. Source file: `scripts/cost-model-output.json`. Guard test: `src/costAnalysis.test.ts`.*
