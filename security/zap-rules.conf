# ZAP scan rules configuration
# Format: <rule_id>	<IGNORE|WARN|FAIL>	<optional comment>
#
# Pass this file to ZAP via the -c flag in scripts/scan-dast.mjs.
# Suppressions here correspond to accepted-risk findings that have been
# deliberately reviewed and documented. NEVER suppress CRITICAL or HIGH
# findings without a written justification.
#
# Severity mappings: FAIL = cause ZAP to exit non-zero, WARN = note only, IGNORE = suppress
#
# --- Accepted risks (verified, documented) ---

# ZAP_10049 — Storable and Cacheable Content
# Risk: INFO. Accepted: Cache-Control: no-store is now set via applyBaseHeaders
# (Phase 2.3, commit 8b1a2d8). ZAP's passive scan may still flag this on the
# first scan before the rule engine fully parses the response headers.
# If this finding re-appears after Phase 2.3, re-verify with curl -i and
# remove this suppression.
10049	IGNORE	Cache-Control no-store now set by applyBaseHeaders — verify with curl before suppressing permanently
